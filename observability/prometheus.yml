# Prometheus config - receives OTLP from collector, scrape config for future targets
# Metrics flow: OTel Collector -> Prometheus (OTLP) -> Grafana
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
